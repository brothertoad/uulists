<section class="hero">
  <header class="hero-body">
    <h1 class="title">{{ title }}</h1>
    {% if not atHome %}
      <div class="tc">
        <a class="embedded-link" href="/">Home</a>
      </div>
    {% endif %}
  </header>
  <hr>
</section>
